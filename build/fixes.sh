# Seems like after libraries updates bug is fixed and we don't need this file anymore
# sed -i  -e 's/if(numberLength != byteLength)throw RangeError(WRONG_LENGTH);//g' 'node_modules/core-js/modules/$.buffer.js'
