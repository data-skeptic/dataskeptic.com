# Language setting http://docs.shippable.com/ci/languages/node/
language: node_js
node_js:
  - 7.4.0
# use this to control what branches get built.
# http://docs.shippable.com/ci/advancedOptions/branches/
branches:
  only:
    - dev

# using pre-defined build variables
# full list http://docs.shippable.com/ci/advancedOptions/environmentVariables/
env:
  global:
    - TEST_RESULTS_DIR=$SHIPPABLE_REPO_DIR/shippable/testresults
    - CODE_COVERAGE_DIR=$SHIPPABLE_REPO_DIR/shippable/codecoverage
    - TESTS_LOC_DIR=$SHIPPABLE_REPO_DIR/tests
    - MOD_LOC=$SHIPPABLE_REPO_DIR/node_modules/.bin/

build:

  # http://docs.shippable.com/ci/shippableyml/#ci
  ci:
    # npm mirrors can sometimes be flacky, better to use shippable_retry
    # http://docs.shippable.com/ci/advancedOptions/retry/
    - shippable_retry npm install
    - shippable_retry npm run build
    - aws s3 cp s3://thespoon/config/config.json ./config/config.json
    - docker build -t dataskeptic-rep .
    - docker tag dataskeptic-rep:latest 189353835613.dkr.ecr.us-west-1.amazonaws.com/dataskeptic-rep:latest
  post_ci:
    - docker push 189353835613.dkr.ecr.us-west-1.amazonaws.com/dataskeptic-rep:latest
    - echo "versionName=latest" >> $JOB_STATE/shipdemo-img.env
    - echo "buildNumber=$BUILD_NUMBER" >> $JOB_STATE/shipdemo-img.env

# Integrations are used to connect external resources to CI
# http://docs.shippable.com/integrations/overview/
integrations:
  hub:
    - integrationName: ECR_INT
      region: us-west-1
      type: ecr
  notifications:
    - integrationName: email
      type: email
      recipients:
        - gleb@thespoon.co
        - bogdan@thespoon.co
      branches:
        only:
          - dev
      on_success: always
      on_failure: always